using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Debug : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        print("Just a lil try try try~~~");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
